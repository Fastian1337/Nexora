# ============================================
# Nexora Platform — Frontend Development Dockerfile
# ============================================
# Development container with hot-reloading

FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 3000

ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

CMD ["npm", "run", "dev"]
